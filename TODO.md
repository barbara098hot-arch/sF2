# TODO

- [x] Atualizar `src/pages/Home.tsx` para carregar `fiorella_config.homeBanners` do `localStorage` no `useEffect` e usar fallback para `defaultHomeBanners`.

- [ ] Testar local: salvar banners no AdminDashboard e abrir `/` para validar que as imagens mudaram.

